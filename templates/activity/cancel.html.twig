{% extends 'base.html.twig' %}

{% block title %}activity details{% endblock %}

{% block h2 %}
    Annuler une sortie
{% endblock %}

{% block body %}
<p>Titre : {{ activity.activityName }}</p>
 {{ form_start(form) }}

<div>
    {{ form_label(form.cancellationReason) }}
    {{ form_widget(form.cancellationReason) }}
    {{ form_errors(form.cancellationReason) }}
</div>
    <button type="submit" class="btn btn-info "> Enregistrer </button>
    <a href="{{ path('activity_list') }}" class="btn btn-secondary">Annuler</a>
    {{ form_end(form) }}
{% endblock %}